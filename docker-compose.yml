version: '3'
services:
    worker:
        build:
            context: .
            dockerfile: ./Dockerfile
        
        env_file:
            - ./env
        
        volumes:
            - /etc/localtime:/etc/localtime:ro
            - /var/run/docker.sock:/var/run/docker.sock
        
        restart: always